<UserControl x:Class="PowerBIOptimizer.UI.Controls.QueryListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView x:Name="ItemsList" 
                  SelectionChanged="ItemsList_SelectionChanged"
                  SelectionMode="Single"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            
            <ListView.View>
                <GridView>
                    <!-- Name Column -->
                    <GridViewColumn Header="Name" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Width="16" 
                                           Height="16" 
                                           Margin="0,0,5,0">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="SQL">
                                                        <Setter Property="Source" Value="/Resources/Images/sql.png"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="DAX">
                                                        <Setter Property="Source" Value="/Resources/Images/dax.png"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="M Query">
                                                        <Setter Property="Source" Value="/Resources/Images/mquery.png"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <TextBlock Text="{Binding Name}" 
                                             VerticalAlignment="Center"
                                             ToolTip="{Binding Name}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Type Column -->
                    <GridViewColumn Header="Type" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Type}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Table Column -->
                    <GridViewColumn Header="Table" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TableName}" 
                                         ToolTip="{Binding TableName}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>

            <!-- Context Menu -->
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Optimize" 
                              Click="Optimize_Click">
                        <MenuItem.Icon>
                            <Image Source="/Resources/Images/optimize.png" 
                                   Width="16" 
                                   Height="16"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="View Details" 
                              Click="ViewDetails_Click">
                        <MenuItem.Icon>
                            <Image Source="/Resources/Images/details.png" 
                                   Width="16" 
                                   Height="16"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="Copy" 
                              Click="Copy_Click">
                        <MenuItem.Icon>
                            <Image Source="/Resources/Images/copy.png" 
                                   Width="16" 
                                   Height="16"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </ListView.ContextMenu>

            <!-- Empty State Template -->
            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <Grid>
                            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                        VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                            <TextBlock Text="No items found"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Foreground="{StaticResource SecondaryBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Items.Count, 
                                                         RelativeSource={RelativeSource AncestorType=ListView}}" 
                                                       Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </ListView.Template>

            <!-- Item Container Style -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F5F5F5"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</UserControl>
